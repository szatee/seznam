const { createTheme } = require('@mui/material/styles');

const palette = {
  primary: {
    main: '#0097a7',
  },
  secondary: {
    main: '#ff8a80',
  },
};

export const theme = createTheme({
  palette,
});
